# Salin file ini menjadi `.env` lalu isi dengan cookie session Instagram milik Anda.
# Nilai sessionid ditemukan di header request saat sedang login di instagram.com.

INSTAGRAM_SESSION_ID=""

# Interval jeda default antar request (ms). Override via `--delay-ms` bila diperlukan.
SCRAPER_DELAY_MS=1500
